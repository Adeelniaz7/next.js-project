import React from 'react';

export default function CreatePostPage() {
  return (
    <div>
      <h1>Create a New Post</h1>
      {/* Post creation is handled via a modal on the topic page, but this page exists to prevent build errors. */}
    </div>
  );
}
